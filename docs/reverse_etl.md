# Reverse ETL — Shared Context (Single Source of Truth)

> **Authoritative environment block for Reverse-ETL / Activation Steps 32–37.**
> Every downstream step (Create Synced Tables, Design/Build/Wire/Deploy Analytics)
> MUST read values from this file instead of re-deriving or inlining them.
>
> **Generated by:** Step 32 — Plan Synced Tables (`activation_table_design`)
> **Status:** Synced tables CREATED, migrated to `subject_maintenanceengineering_test.an_maintenanceengineering_ods`, and renamed to `qx_ppmtx_synced_gold_*`. Analytics app (`apps_lakebase/nathan-a-qx-predictive`) wired to them (Step 37 — `activation_wire_lakebase`) and **DEPLOYED + validated live** to the existing Databricks App `nathan-a-ppmtx` in `adb-620317033646362` (Step 38 — `activation_deploy_validate`). All `/api/analytics/*` endpoints return `source: "live"`.
>
> **Propulsion scoping (Step 39):** The synced tables hold **fleet-wide** data, but this app is propulsion-only. The app's SQL (`server/server.ts`) now filters every endpoint to propulsion scope — ATA chapters `{49 (APU), 70–80 (powerplant/engine)}` for defect endpoints and a part-population subquery (engine-ATA removals + curated PN overrides) for parts/spares/inventory. This replicates the logic of the user's `vw_prop_*` Unity Catalog views (which cannot be consumed by a Postgres app or synced to Lakebase). Live-validated: defects 172,982→5,904; only ATA {49,70–80} appear. See `docs/propulsion_scope_discovery.md` for the authoritative definition and endpoint→filter mapping.
>
> **Supervisor Agent chat tab (Step 40):** A new **Assistant** tab wires the app to the
> **Propulsion-Supervisor-Agent** (Agent Bricks Multi-Agent Supervisor) served at endpoint
> `mas-99316ed5-endpoint` (task `agent/v1/responses`). Wired via the AppKit `serving()` plugin + a
> normalizing `POST /api/agent` route; bound with `CAN_QUERY` and invoked **on-behalf-of the signed-in
> user (OBO)** because the MAS's Genie subagent needs the user's UC/Genie access (the app SP can't be
> granted it on the governed catalog). **A one-time in-browser app-consent for the new scopes
> (`serving.serving-endpoints, dashboards.genie, sql`) is required before the tab returns live answers.**
> See `docs/supervisor_agent_discovery.md`.

## Environment Block

```
workspace_url:              https://adb-620317033646362.2.azuredatabricks.net   # where Lakebase + synced tables live
lakehouse_default_catalog:  subject_maintenanceengineering_test
lakehouse_gold_schema:      an_maintenanceengineering_ods        # where the qx_ppmtx_gold_* Delta source tables live
gold_table_prefix:          qx_ppmtx_gold_                       # Gold (Delta) source-table prefix
synced_table_prefix:        qx_ppmtx_synced_gold_                # Postgres synced-table prefix ("synced" moved after qx_ppmtx)
user_schema_prefix:         an_maintenanceengineering_ods        # Postgres schema holding the synced tables
user_app_name:              nathan-a-ppmtx      # also Lakebase project_id
lakebase_instance_name:     nathan-a-ppmtx
lakebase_postgres_database: databricks_postgres  # fixed Lakebase default DB
lakebase_postgres_schema:   an_maintenanceengineering_ods        # target schema; NOT public, NOT n_archibald
lakebase_root_branch:       production            # root branch is always production
endpoint_name:              projects/nathan-a-ppmtx/branches/production/endpoints/primary
lakebase_mode:              autoscaling           # workshop is Autoscaling-only
lakebase_host:              ep-summer-wave-eey39uw9.database.westus2.azuredatabricks.net

# Analytics app (consumes the synced tables)
app_dir:                    apps_lakebase/nathan-a-qx-predictive
deployed_app_name:          nathan-a-ppmtx      # existing Databricks App updated in place (bundle name + resource key "app")
app_deploy_workspace_url:   https://adb-620317033646362.2.azuredatabricks.net   # co-located with Lakebase (caveat resolved)
app_deploy_profile:         adb-620317033646362
app_url:                    https://nathan-a-ppmtx-620317033646362.2.azure.databricksapps.com
app_sp_client_id:           06a44e88-0780-4525-9d65-ff609515cdc4   # granted USAGE+SELECT on the synced schema
local_dev_profile:          adb-620317033646362   # local `npm run dev` must auth here to mint the Lakebase OAuth token

# Cost controls (workshop defaults; every later step MUST respect these)
autoscaling_limit_min_cu:     0.5                 # floor compute; allows scale-to-zero
autoscaling_limit_max_cu:     2.0                 # ceiling compute; do NOT raise during workshop
suspend_timeout_duration:     1800s               # idle -> suspend after 30 minutes
endpoint_type:                ENDPOINT_TYPE_READ_WRITE
sync_mode_allowlist:          ["SNAPSHOT", "TRIGGERED"]   # CONTINUOUS is banned for cost reasons
triggered_min_cron_interval:  24h                 # TRIGGERED cadence floor
triggered_cron_default:       0 0 6 * * ?         # Quartz cron: daily 06:00 (>= 24h)
```

## Synced Tables (Postgres schema `an_maintenanceengineering_ods`)

All 13 Gold tables were synced, migrated, and renamed with the `qx_ppmtx_synced_gold_` prefix
("synced" moved after `qx_ppmtx`, per the naming convention `qx_ppmtx_synced_gold_<table>`):

| # | Synced table (Postgres) | Approx rows |
|---|-------------------------|-------------|
| 1 | `qx_ppmtx_synced_gold_dim_date` | 5,844 |
| 2 | `qx_ppmtx_synced_gold_dim_part` | 89,344 |
| 3 | `qx_ppmtx_synced_gold_dim_aircraft` | 120 |
| 4 | `qx_ppmtx_synced_gold_dim_station` | 129 |
| 5 | `qx_ppmtx_synced_gold_dim_ata_chapter` | 1,142 |
| 6 | `qx_ppmtx_synced_gold_fact_defect` | 172,982 |
| 7 | `qx_ppmtx_synced_gold_fact_component_removal` | 751,774 |
| 8 | `qx_ppmtx_synced_gold_fact_inventory_transaction` | 13,950,200 |
| 9 | `qx_ppmtx_synced_gold_fact_inventory_snapshot` | 147,532 |
| 10 | `qx_ppmtx_synced_gold_fact_inventory_control` | 638,264 |
| 11 | `qx_ppmtx_synced_gold_fact_order` | 1,406,198 |
| 12 | `qx_ppmtx_synced_gold_fact_teardown` | 78,372 |
| 13 | `qx_ppmtx_synced_gold_bridge_defect_part` | 44,334 |

Full column/type/NULL/enum profiles: see `docs/lakebase_schema_discovery.md`.

## ✅ Cross-Workspace Deploy Caveat — RESOLVED

The Lakebase endpoint and synced tables live in workspace **`adb-620317033646362`**. The app was
**deployed into the same workspace** (`adb-620317033646362`, option (a)) by aligning the bundle
identity (`bundle.name: nathan-a-ppmtx`, `resources.apps.app.name: nathan-a-ppmtx`, target host
`adb-620317033646362`) with the pre-existing `nathan-a-ppmtx` app, so `databricks bundle deploy`
updated it in place. The app's `databricks.yml` now targets `adb-620317033646362`.
The app SP (`06a44e88-0780-4525-9d65-ff609515cdc4`) was granted `USAGE` on the schema and `SELECT`
on all 13 synced tables (plus `ALTER DEFAULT PRIVILEGES ... GRANT SELECT`) via a serverless job —
the schema owner is the superuser role `nathan-archibald`. After the grant, every `/api/analytics/*`
endpoint returns `source: "live"` and `/api/health/lakebase` reports `connected: true`.

## Notes / Provenance

- **Source catalog verified against the live workspace** (not the design docs): all 13
  `qx_ppmtx_gold_*` tables exist in `subject_maintenanceengineering_test.an_maintenanceengineering_ods`
  as **MANAGED Delta** tables with **`delta.enableChangeDataFeed = true`**.
  (The Gold design YAML said `subject_maintenanceengineering`; that value is stale — the
  deployed tables use the `_test` catalog from the bundle variable `catalog`.)
- **CDF is already enabled on every source table**, so Step 33 does **not** need to run
  `ALTER TABLE ... SET TBLPROPERTIES (delta.enableChangeDataFeed = true)` — TRIGGERED sync is valid immediately.
- **Target Postgres schema is `an_maintenanceengineering_ods`** (custom; not `public`, not the earlier `n_archibald`).
  The tables were originally synced into `n_archibald` and later moved/renamed to their current location.
- **Endpoint verified** via `databricks postgres get-endpoint` — round-tripped
  `autoscaling_limit_min_cu=0.5`, `autoscaling_limit_max_cu=2.0`, `suspend_timeout_duration=1800s`,
  `current_state=ACTIVE`.
- **One project per student** — only the auto-created `production` branch + `primary` endpoint are used.
