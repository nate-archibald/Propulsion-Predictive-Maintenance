# Activation — Synced Table Plan

> **Per-candidate reverse-ETL sync plan** for Step 33 (Create Synced Tables).
> Environment values (catalog, schema, host, cost caps, cron) live in
> [`reverse_etl.md`](reverse_etl.md) — this file does **not** re-inline them.
>
> **Source:** `subject_maintenanceengineering_test.an_maintenanceengineering_ods.qx_ppmtx_gold_*`
> **Target:** Lakebase project `nathan-a-ppmtx`, database `databricks_postgres`, schema `an_maintenanceengineering_ods` (post-migration; originally synced into `n_archibald`, then moved/renamed)
> **Generated by:** Step 32 — Plan Synced Tables (`activation_table_design`)
> **Status (updated):** All 13 tables created, migrated to `subject_maintenanceengineering_test.an_maintenanceengineering_ods`, and renamed to the `qx_ppmtx_synced_gold_*` convention. Consumed by the analytics app `apps_lakebase/nathan-a-qx-predictive`.

## Summary

| Attribute | Value |
|---|---|
| Candidates | **13** (5 dimensions, 7 facts, 1 bridge) |
| Sync mode (all) | **TRIGGERED** — every source is MANAGED Delta with CDF enabled |
| CONTINUOUS candidates | **0** (banned) |
| Cron (all) | `0 0 6 * * ?` — daily 06:00, ≥ 24h floor satisfied |
| Type mitigation needed | **None** — all columns are scalar (BIGINT / INT / STRING / DECIMAL / DATE) |
| Synced name convention | `qx_ppmtx_synced_gold_<table>` — "synced" is placed after `qx_ppmtx` (e.g. `qx_ppmtx_gold_dim_part` → `qx_ppmtx_synced_gold_dim_part`) |

> **Why TRIGGERED for all:** each source is a MANAGED **Delta** table with
> `delta.enableChangeDataFeed = true` (verified live), and the Gold layer refreshes on a
> daily merge cadence. TRIGGERED with a ≥24h cron gives incremental, cost-controlled syncs.
> SNAPSHOT was reserved for non-Delta sources (Metric Views / TVFs / Iceberg) — **none are in scope here**.

## Candidate Table (creation order)

Order = **dimensions → facts → bridge** so foreign-key targets exist before dependents.
(Databricks Synced Tables do not enforce FKs, but this order keeps the target consistent.)

| # | Source table (`…_test.an_maintenanceengineering_ods.<t>`) | Synced name (`an_maintenanceengineering_ods.<t>`) | Entity | Primary key | Mode | Cron | CDF |
|---|---|---|---|---|---|---|---|
| 1 | `qx_ppmtx_gold_dim_date` | `qx_ppmtx_synced_gold_dim_date` | dimension | `dim_date_key` (INT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 2 | `qx_ppmtx_gold_dim_part` | `qx_ppmtx_synced_gold_dim_part` | dimension | `dim_part_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 3 | `qx_ppmtx_gold_dim_aircraft` | `qx_ppmtx_synced_gold_dim_aircraft` | dimension | `dim_aircraft_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 4 | `qx_ppmtx_gold_dim_station` | `qx_ppmtx_synced_gold_dim_station` | dimension | `dim_station_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 5 | `qx_ppmtx_gold_dim_ata_chapter` | `qx_ppmtx_synced_gold_dim_ata_chapter` | dimension | `dim_ata_chapter_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 6 | `qx_ppmtx_gold_fact_defect` | `qx_ppmtx_synced_gold_fact_defect` | fact | `fact_defect_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 7 | `qx_ppmtx_gold_fact_component_removal` | `qx_ppmtx_synced_gold_fact_component_removal` | fact | `fact_component_removal_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 8 | `qx_ppmtx_gold_fact_inventory_transaction` | `qx_ppmtx_synced_gold_fact_inventory_transaction` | fact | `fact_inventory_transaction_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 9 | `qx_ppmtx_gold_fact_inventory_snapshot` | `qx_ppmtx_synced_gold_fact_inventory_snapshot` | fact | `fact_inventory_snapshot_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 10 | `qx_ppmtx_gold_fact_inventory_control` | `qx_ppmtx_synced_gold_fact_inventory_control` | fact | `fact_inventory_control_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 11 | `qx_ppmtx_gold_fact_order` | `qx_ppmtx_synced_gold_fact_order` | fact | `fact_order_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 12 | `qx_ppmtx_gold_fact_teardown` | `qx_ppmtx_synced_gold_fact_teardown` | fact | `fact_teardown_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |
| 13 | `qx_ppmtx_gold_bridge_defect_part` | `qx_ppmtx_synced_gold_bridge_defect_part` | bridge | `bridge_defect_part_key` (BIGINT) | TRIGGERED | `0 0 6 * * ?` | enabled |

## Per-candidate CDF notes

- **All 13 candidates:** source has `delta.enableChangeDataFeed = true` **already set** (verified via
  `databricks tables list`). Step 33 must **NOT** re-enable CDF — it is a no-op and can error on MANAGED tables.
- **No "CDF not applicable" rows:** there are no Metric Views, TVFs, or Iceberg sources in this plan,
  so no SNAPSHOT-only / non-Delta candidates exist.

## Type-mapping notes

| Lakehouse type (source) | Postgres type (target) | Action |
|---|---|---|
| `BIGINT` | `bigint` | direct |
| `INT` | `integer` | direct |
| `STRING` | `text` | direct |
| `DECIMAL` | `numeric` | direct |
| `DATE` (`dim_date.calendar_date`) | `date` | direct |

- **No complex types** (ARRAY / MAP / STRUCT) and **no unsupported types**
  (GEOGRAPHY / GEOMETRY / VARIANT / OBJECT) appear in any of the 13 tables — **no JSONB casting or column drops required**.

## Cost-control compliance (re-stated for Step 33)

- Endpoint capped at `min_cu=0.5 / max_cu=2.0`, `suspend_timeout_duration=1800s` — do NOT raise.
- Every candidate is **TRIGGERED** with a **daily (≥24h)** cron — no sub-daily schedules.
- **CONTINUOUS is banned** and unused.
- Single project / `production` branch / `primary` endpoint only.

## Done / Next

- ✅ `reverse_etl.md` + this plan authored; Lakebase project `nathan-a-ppmtx` provisioned & capped.
- ⏭️ **Step 33 — Create Synced Tables:** create the 13 synced tables in the order above via
  `databricks postgres create-synced-table`, targeting schema `an_maintenanceengineering_ods`, then poll each to a healthy
  `ONLINE_*` state and verify row counts. **Do not create them in this step.**
